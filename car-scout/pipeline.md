# Deal pipeline

One row per deal worth acting on. The scout adds `surfaced` rows; you own
everything after that. Statuses: `surfaced → messaged → seen it → offered →
bought → listed → sold` (or `dead` with a reason).

| Date | Vehicle | Link | Ask | Est. value | Spread | Status | Outcome / notes |
|------|---------|------|-----|-----------|--------|--------|-----------------|
