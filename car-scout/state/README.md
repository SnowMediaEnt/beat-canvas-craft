# State

`seen.jsonl` lives here — one JSON line per listing the scout has already
surfaced (`{id, firstSeen, price, title, url}`), so daily runs only show you
new listings and real price drops. The scout creates and maintains it; safe
to delete if you want a fresh start.
